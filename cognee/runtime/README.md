# Cognee Runtime

This directory is reserved for local Cognee runtime state: generated env files,
runtime databases, provider-specific instances, and local service data.

The contents are intentionally ignored by Git. Recreate them with the Cognee
setup and ingest scripts instead of committing them.
