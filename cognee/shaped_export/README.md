# Shaped Cognee Export

This directory is used for generated shaped document packs for Cognee ingestion.

The generated contents are ignored by Git. Recreate them with:

```bash
cognee/scripts/cognee_ingest_simple.py --shape-only
```
