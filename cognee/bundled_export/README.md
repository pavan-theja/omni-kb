# Bundled Cognee Export

This directory is used for generated bundled Cognee export files.

The generated contents are ignored by Git. Recreate them with:

```bash
processed_kb_docs/dump_canonical_bundles.py
```
