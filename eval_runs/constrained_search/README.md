# Constrained Search Eval Runs

Live constrained-search E2E eval artifacts are written here by:

```bash
.venv/bin/python cognee/scripts/constrained_search/eval_runner.py
```

Each run gets its own timestamped directory. Generated run directories are local
evidence artifacts; review them before deciding what to keep.
