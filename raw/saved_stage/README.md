# Saved Runtime Stages

This directory is reserved for local saved runtime stages and database snapshots.

The contents are ignored by Git because they are large generated state. Recreate
or re-export them locally when needed.
